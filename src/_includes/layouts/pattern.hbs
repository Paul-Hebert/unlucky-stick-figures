---
layout: pattern-library
---

{{{ content }}}

{{#each collections.patternExamples}}
  {{#if (rootPathsMatch this.inputPath ../page.inputPath)}}
    {{~> layouts/pattern-example}}
  {{/if}}
{{/each}}